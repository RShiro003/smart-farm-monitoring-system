# 스마트팜 모니터링 시스템 개발 발표 브리프

> 이 문서는 지금까지 진행한 개발 작업을 발표자료로 옮기기 위한 요약본이다.  
> 발표의 핵심 메시지는 **“센서값을 보여주는 대시보드에서, 이상 상황을 놓치지 않고 안전하게 대응할 수 있는 운영 시스템으로 발전시켰다”**이다.

## 1. 프로젝트와 이번 개발의 목적

### 프로젝트 목적

- ESP32에 연결된 온도·습도·토양 수분·조도 센서값을 원격 수집한다.
- 사용자가 웹 대시보드에서 현재 상태와 과거 추이를 확인할 수 있게 한다.
- 작물별 적정 생육 범위에서 벗어난 상황을 신속하게 감지하고 Discord로 알린다.
- 여러 센서 노드와 재배 구역을 한 시스템에서 관리한다.
- 원격 장치 제어 없이 센서 관측, 이상 알림, 이력 관리에 집중한다.

### 이번 개발의 목적

기존 시스템은 측정과 화면 표시에는 강점이 있었지만, 실제 운영 중 장애·통신 실패·설정 불일치가 발생했을 때 사용자가 이를 확실히 인지하고 대응하기에는 부족했다. 이번 개발은 다음 네 가지 방향에 집중했다.

1. **관측 가능성 강화**: 알림 이력, 장치 상태, 시스템 상태를 웹에서 확인한다.
2. **판정 일관성 확보**: 작물 가이드, 서버 알림, ESP32 LED가 동일한 임계값을 사용하게 한다.
3. **신뢰성 강화**: 통신 실패 시 센서 데이터와 Discord 알림이 유실되지 않게 한다.
4. **운영·보안 강화**: 인증, 데이터 보존, 배포 구성, 자동 테스트를 갖춘다.

## 2. 기존 문제와 요구사항

### 핵심 문제 1: 알림 기록은 저장되지만 조회할 수 없음

- 서버는 이상 및 복구 이벤트를 `event_log` 테이블에 계속 저장하고 있었다.
- 그러나 이를 읽는 API와 화면이 없어 Discord 메시지를 놓치면 과거 상황을 확인하기 어려웠다.
- 요구사항: 장치·기간·상태·측정 항목별 알림 이력 조회 API와 대시보드 섹션 제공.

### 핵심 문제 2: 작물 프로필과 실제 판정 임계값의 이원화

- 작물을 선택하면 화면의 생육 가이드와 차트 기준선만 변경됐다.
- ESP32 LED와 Discord 알림은 별도의 `threshold_settings` 값을 계속 사용했다.
- 사용자는 작물 변경이 실제 판정에도 반영되었다고 오해할 수 있었다.
- 요구사항: 작물 권장 범위를 장치 임계값으로 명시적으로 적용하고, 두 값의 일치 여부를 표시.

### 핵심 문제 3: 장치 오프라인을 감지하지 못함

- ESP32 전원, Wi-Fi 또는 네트워크가 끊기면 새 요청 자체가 오지 않는다.
- 대시보드는 마지막 수신값을 계속 보여주므로 현재값으로 오인할 수 있었다.
- 요구사항: 마지막 수신 시각 기반 온라인/오프라인 판정, 화면 배지, 무응답 및 복구 알림.

### 핵심 문제 4: 조도값의 의미와 단위 불일치

- 실제 센서 노드는 디지털 조도 모듈의 `0/1`을 전송했지만 화면은 이를 `lux`로 표시했다.
- lux 임계값과 디지털값을 비교하면 판정 자체가 의미가 없었다.
- 요구사항: 측정 단위를 구분하고 lux일 때만 lux 임계값을 적용.

### 추가 점검에서 발견한 운영상 문제

- 모든 API가 사실상 무인증으로 노출될 수 있었다.
- Discord 전송이 일시 실패하면 알림이 유실될 수 있었다.
- ESP32 재전송 시 같은 측정값이 중복 저장될 수 있었다.
- DHT 센서 하나가 실패하면 정상인 토양·조도 데이터도 함께 버려질 수 있었다.
- 원시 센서 데이터가 무기한 쌓여 장기 운영 시 DB가 과도하게 커질 수 있었다.
- Flask 개발 서버와 CDN 의존 구성은 실제 배포에 적합하지 않았다.
- 입력값의 NaN, 무한대 및 물리 범위에 대한 검증이 부족했다.

## 3. 기존 구현했던 기능

이번 개선 이전에도 프로젝트에는 다음과 같은 스마트팜 기본 기능이 구현되어 있었다.

- ESP32 더미 노드와 실제 센서 노드에서 주기적으로 센서값 수집
- Flask REST API를 통한 센서 데이터 수신 및 SQLite 저장
- 온도, 습도, 토양 수분, 조도 최신값 표시
- 기간별 통계, 차트, 측정 이력 조회
- 장치별 데이터 구분 및 대시보드 장치 선택
- 장치별 임계값 저장과 ESP32 임계값 조회
- 임계값 이탈 및 정상 복구 시 Discord 알림 전송
- 작물 프로필 등록·수정·삭제 및 권장 생육 범위 표시
- 생육 기록 관리와 일별 환경 분석
- 토양 수분 변화 기반 관수 이벤트 감지 및 과거 데이터 재분석
- 토양 수분 센서 건조/습윤 원시값 보정

## 4. 이번에 추가한/수정한 기능

### 4.1 알림 이력 조회

- `GET /api/events` API를 추가했다.
- 장치, 상태, 측정 항목, 날짜, 시작·종료 시각 필터를 지원한다.
- 기존 `/api/dashboard/events`는 호환 경로로 유지했다.
- 대시보드에 **알림 기록** 표를 추가해 이상, 복구, 장치 오프라인 이력을 확인할 수 있게 했다.
- 현재 필터 조건과 동일한 알림 이력을 CSV로 내려받을 수 있다.

### 4.2 작물 프로필과 실제 임계값 연동

- 대시보드에 **작물 기준 적용** 기능을 추가했다.
- `POST /api/thresholds/from-crop`가 선택 작물의 권장 범위를 해당 장치의 실제 임계값으로 저장한다.
- 임계값과 장치-작물 연결을 하나의 DB 트랜잭션으로 처리한다.
- 하드웨어마다 다른 토양 센서 보정값은 작물 적용 시 덮어쓰지 않는다.
- 화면에서 현재 임계값과 선택 작물의 권장 범위가 일치하는지 표시한다.
- 결과적으로 대시보드 기준선, 서버 알림, ESP32 LED 판정의 차이를 사용자가 명확히 통제할 수 있다.

### 4.3 장치 온라인/오프라인 감지

- 장치별 마지막 수신 시각을 기준으로 상태를 계산한다.
- 기본 120초 동안 수신이 없으면 오프라인으로 판정하며 환경변수로 조정할 수 있다.
- 아직 한 번도 측정값을 보내지 않은 등록 장치도 유예시간 후 오프라인으로 감지한다.
- 백그라운드 watchdog가 무응답 및 복구 이벤트를 기록하고 Discord 알림을 생성한다.
- 대시보드에 장치별 상태와 전체 오프라인 장치 수를 배지로 표시한다.

### 4.4 조도 단위 정합성 개선

- 센서 데이터에 `light_unit`을 저장해 `lux`와 `digital`을 구분한다.
- lux 측정값만 lux 임계값 및 조도 알림에 사용한다.
- 디지털 조도는 화면에서 밝음/어두움과 원시값으로 표시하고, lux 기반 적정 판정은 생략한다.
- 기존의 의미 없는 기본 범위 `0~100`은 안전한 lux 전체 범위 `0~200000`으로 한 번만 마이그레이션한다.
- 현재 실제 노드는 디지털 조도 방식이며, BH1750 등 lux 센서 도입 전까지 잘못된 조도 경보가 발생하지 않는다.

### 4.5 Discord 알림 전달 신뢰성

- 이벤트 기록과 알림 발송 요청을 트랜잭션으로 함께 저장하는 `notification_outbox`를 추가했다.
- 전송 실패 메시지는 상태와 시도 횟수를 보존하고 지수형 지연 후 재시도한다.
- 전송 중 프로세스가 중단된 오래된 작업도 다시 회수한다.
- `/api/status`에서 대기·실패 알림 수와 watchdog 상태를 확인할 수 있다.
- 임계값 이탈이 지속될 때의 재알림 간격과 항목별 활성 여부를 장치별 또는 전역으로 설정할 수 있다.
- Discord webhook 값은 조회 API나 화면으로 다시 노출하지 않고 설정 여부만 반환한다.

### 4.5.1 지속 시간·연속 횟수·위험 단계 알림

- 순간 센서 노이즈가 바로 알림이 되지 않도록 이상 연속 횟수와 최소 지속 시간을 설정할 수 있다.
- 정상 범위 복귀도 지정된 연속 횟수를 만족해야 복구로 확정한다.
- 정상 범위 폭 대비 이탈률로 경고와 위험을 구분한다.
- 큰 폭의 위험 값은 설정에 따라 확인 대기 없이 즉시 Discord로 전달한다.
- 대시보드 알림 기록과 CSV에도 경고·위험 단계가 함께 저장된다.

### 4.5.2 일간·주간 Discord 요약

- 장치별 또는 전체 기본 설정으로 일간·주간 요약을 활성화할 수 있다.
- 발송 시각과 주간 발송 요일을 대시보드에서 설정한다.
- 센서 평균·최저·최고, 수집 및 오류 건수, 이상·위험·복구·오프라인 건수를 요약한다.
- 기간별 요약은 한 번만 큐에 들어가며 전송 실패 시 기존 outbox 정책으로 재시도한다.

### 4.6 센서 수집 안정성 및 데이터 품질

- ESP32 측정마다 부팅 식별자와 순번으로 구성한 `sample_id`를 부여한다.
- 서버는 `(device_id, sample_id)`를 유일하게 관리해 재전송을 중복 저장하지 않는다.
- 실제 ESP32 노드는 최대 30개 측정값을 메모리 큐에 보관하고 통신 복구 후 재전송한다.
- DHT 온습도 읽기 실패 시 온도·습도는 `null`, 오류 원인은 `sensor_errors`로 보내며 정상인 토양·조도값은 계속 보존한다.
- 센서값, 임계값, 작물값에 유한수 검사와 물리적 허용 범위 검사를 추가했다.
- 장치 ID와 문자열 입력에도 길이 및 허용 문자 검증을 적용했다.

### 4.7 장치 및 운영 관리

- 장치 ID에 이름, 위치, 메모를 붙이는 장치 레지스트리를 추가했다.
- 등록 장치와 실제 센서 데이터에서 발견된 장치를 합쳐 기존 설치와 호환되게 했다.
- 장치 메타데이터 삭제 시 센서 이력은 보존한다.
- 센서 및 알림 이력을 UTF-8 BOM CSV로 스트리밍 내보낼 수 있게 했다.
- 대용량 내보내기는 최대 행 수를 제한해 브라우저와 서버 메모리를 보호한다.
- 급수·환기·비료 투입·작물 점검·센서 점검 등의 사용자 작업을 장치별로 기록한다.
- 작업 시각을 센서 차트에 보라색 세로선으로 표시해 환경 변화와 현장 조치의 관계를 확인한다.

### 4.8 장기 데이터 보존 및 조회 최적화

- 완료된 시간 구간을 평균·최솟값·최댓값으로 집계하는 `sensor_data_hourly`를 추가했다.
- 기본 정책은 원시 데이터 90일, 시간별 집계 730일 보관이며 환경변수로 변경할 수 있다.
- 원시 데이터 삭제 전 집계 존재 여부를 확인해 집계되지 않은 데이터가 사라지지 않게 했다.
- 오래된 차트 및 일별 분석은 시간별 집계를 사용하고, 최근 구간은 원시 데이터를 사용한다.
- 전체 조회와 생육 기록 등에 상한을 두어 과도한 응답을 방지했다.
- 대시보드의 중복 주기 요청을 변경 시각 기반 갱신으로 통합하고, 숨겨진 브라우저 탭에서는 폴링을 멈춘다.

### 4.9 인증과 웹 보안

- 관리자용 전역 API 키와 장치별 API 키를 분리했다.
- 장치 키는 자신의 센서 전송과 임계값 조회만 수행할 수 있다.
- 관리·설정·내보내기 API는 인증을 강제한다.
- 대시보드는 키를 12시간 유효한 `HttpOnly`, `SameSite=Strict` 세션 쿠키로 교환하며 브라우저 저장소에 원본 키를 남기지 않는다.
- 로그인 실패 횟수 제한, 최대 요청 크기, 보안 응답 헤더, 선택적 프록시 신뢰 설정을 추가했다.
- webhook과 실제 Wi-Fi/API 비밀값은 Git에 포함하지 않도록 예제 파일과 `.gitignore`를 정리했다.

### 4.10 모니터링 전용 범위 정리

- 관수 펌프, 환기 팬, 보광등을 원격 또는 자동으로 작동시키는 서버 API와 서비스 코드를 제거했다.
- 대시보드의 장비 제어 카드와 ON/OFF·자동 모드 UI를 제거했다.
- ESP32 펌웨어의 릴레이 GPIO, 명령 폴링 및 원격 제어 로직을 제거했다.
- 토양 수분 변화로 관수 발생 여부를 추정하는 기능은 장치를 작동시키지 않는 센서 분석이므로 유지했다.
- 시스템의 역할을 **측정 → 임계값 판정 → Discord 알림 → 대시보드 기록**으로 명확히 제한했다.

### 4.11 배포와 유지보수 개선

- 개발용 Flask 서버 대신 Waitress로 실행할 수 있게 의존성과 실행 방법을 정리했다.
- Raspberry Pi용 systemd 서비스 템플릿과 `.env.example`을 추가했다.
- DB 상태, 최신 데이터, 디스크 여유 공간, 알림 큐, watchdog 상태를 `/api/status`로 확인한다.
- Chart.js와 annotation 플러그인을 프로젝트 내부 정적 파일로 고정해 CDN 장애와 버전 변경 영향을 줄였다.
- GitHub Actions에서 Python 컴파일·단위 테스트와 실제/더미 ESP32 펌웨어 빌드를 자동 수행한다.

## 5. 검증 결과

### 자동 검증

- Python 단위·통합 테스트: **총 175개 통과**
- Python 소스 컴파일 검사: 통과
- 대시보드 JavaScript 구문 검사: 통과
- Python 패키지 의존성 충돌 검사: 통과
- Git diff 공백 오류 검사: 통과
- 실제 센서 ESP32 PlatformIO 빌드: 통과
  - RAM 약 14.7%, Flash 약 72.4% 사용
- 더미 센서 ESP32 PlatformIO 빌드: 통과
  - RAM 약 14.5%, Flash 약 70.7% 사용

### 주요 회귀 테스트 범위

- 알림 이력 필터와 페이징
- 작물 권장 범위의 장치 임계값 적용
- 온라인, 오프라인, 복구 상태 전이 및 중복 알림 방지
- lux와 digital 조도 판정 분리
- 관리자 키와 장치별 키의 권한 분리
- webhook 비밀값 마스킹
- NaN·무한대·잘못된 범위 입력 거부
- DHT 부분 실패 데이터 저장
- `sample_id` 기반 중복 방지
- Discord outbox 저장 및 재시도
- 원시·시간별 집계 데이터 혼합 조회
- 로컬 Chart.js 정적 파일 제공

### 검증 범위의 한계

- 자동 테스트와 펌웨어 빌드는 통과했지만 실제 농장 환경의 장시간 통신, 전원 노이즈, 센서 편차까지 보장하지는 않는다.
- 실제 센서와 네트워크 환경에서 장시간 수집 및 알림 검증이 추가로 필요하다.
- 외부 인터넷에서 Raspberry Pi까지의 장애는 서버 내부 watchdog만으로 감지할 수 없어 별도 외부 모니터링이 필요하다.

## 6. 아키텍처 또는 처리 흐름

### 전체 구성

```text
[온습도·토양수분·조도 센서]
               │ 측정
               ▼
            [ESP32]
     sample_id 생성 / 재전송 큐
     임계값 기반 LED 상태 표시
               │ HTTP + 장치별 API 키
               ▼
     [Flask + Waitress 서버 / Raspberry Pi]
       ├─ 입력 검증 및 장치 인증
       ├─ 센서 저장·중복 제거
       ├─ 임계값 판정
       ├─ 오프라인 watchdog
       ├─ 알림 outbox 및 재시도
       └─ 시간별 집계·보존 정책
          │                 │
          ▼                 ▼
   [SQLite 데이터베이스]   [Discord Webhook]
          │
          ▼
      [웹 대시보드]
 최신값·차트·알림·장치 상태·설정
```

### 센서 데이터와 알림 처리 흐름

1. ESP32가 센서를 읽고 고유 `sample_id`, `device_id`, `light_unit`, 오류 정보를 포함한다.
2. 서버가 장치별 API 키와 입력값을 검증한다.
3. 같은 `sample_id`의 재전송이면 기존 행을 재사용하고, 새 측정이면 SQLite에 저장한다.
4. 정상적으로 읽힌 측정 항목만 장치 임계값과 비교한다.
5. 정상→이상 또는 이상→복구 상태 전이가 발생하면 이벤트와 알림 발송 작업을 함께 저장한다.
6. 알림 작업자가 Discord로 전송하고, 실패하면 큐에 남겨 재시도한다.
7. 대시보드는 변경 시각을 확인한 뒤 최신값, 차트, 상태, 알림 이력을 갱신한다.

### 오프라인 감지 흐름

```text
마지막 수신 시각 확인
        │
        ├─ 기준 시간 이내 ─→ 온라인
        │                    └─ 이전이 오프라인이면 복구 이벤트
        │
        └─ 기준 시간 초과 ─→ 오프라인
                             └─ 최초 전이일 때 이벤트 + Discord 알림
```

## 7. 시연 순서

### 사전 준비

- 서버를 Waitress로 실행하고 `/api/status`가 HTTP 200인지 확인한다.
- 더미 ESP32 또는 실제 ESP32에 고유 장치 ID와 장치별 API 키를 설정한다.
- Discord 테스트 채널 webhook을 등록한다.
- 발표 시간을 고려해 오프라인 판정 시간을 시연용으로 짧게 설정할 수 있다.

### 권장 시연 시나리오

1. **전체 대시보드 소개**
   - 최신 센서 카드, 장치 선택, 차트, 측정 이력, 생육 정보를 보여준다.

2. **실시간 센서 수신**
   - ESP32 Serial Monitor와 웹 대시보드를 함께 보여준다.
   - 새 측정값이 저장되고 화면이 갱신되는 것을 확인한다.

3. **여러 장치 관리**
   - 장치 이름과 위치를 등록한다.
   - 장치 선택을 바꾸며 데이터와 임계값이 분리되는 것을 보여준다.

4. **작물 프로필과 임계값 연동**
   - 작물을 선택했을 때 먼저 가이드만 바뀌는 점을 설명한다.
   - 현재 임계값과 작물 권장 범위 불일치 안내를 보여준다.
   - **작물 기준 적용** 버튼을 누른 뒤 일치 상태와 실제 임계값 변경을 확인한다.

5. **이상 및 복구 알림**
   - 더미 데이터 또는 센서 조작으로 임계값을 벗어난 값을 입력한다.
   - 대시보드 상태 변화, 알림 기록 추가, Discord 메시지를 확인한다.
   - 정상 범위로 돌려 복구 이벤트도 같은 방식으로 확인한다.

6. **조도 단위 처리**
   - lux 장치는 수치와 임계값 판정을 표시한다.
   - digital 장치는 밝음/어두움으로 표시되고 lux 판정이 생략되는 것을 비교한다.

7. **장치 오프라인과 복구**
   - ESP32 전원 또는 전송을 잠시 중단한다.
   - 오프라인 배지, 알림 기록, Discord 무응답 메시지를 확인한다.
   - 전송을 재개해 온라인 복구와 복구 알림을 확인한다.

8. **통신 실패 후 데이터 복구**
   - 서버 또는 Wi-Fi를 잠시 끊고 ESP32 재전송 큐가 쌓이는 모습을 보여준다.
   - 연결 복구 후 누락 측정이 전송되고 `sample_id` 때문에 중복 저장되지 않음을 설명한다.

9. **운영 관리 기능**
   - 알림 필터와 CSV 다운로드를 보여준다.
   - 저장소 통계와 시간별 집계·보존 정책을 소개한다.
   - `/api/status`에서 DB, 디스크, 알림 큐, watchdog 상태를 확인한다.

## 8. 남은 과제와 향후 계획

### 배포 전 사용자가 마무리할 작업

- 관리자 API 키와 장치별 API 키를 서로 다른 긴 난수로 생성한다.
- Raspberry Pi에 `/etc/smart-farm.env`를 만들고 권한을 서비스 계정으로 제한한다.
- 각 ESP32 프로젝트의 `secrets.example.h`를 `secrets.h`로 복사한 뒤 Wi-Fi, 서버 주소, 장치 ID, 장치 키를 입력한다.
- systemd 서비스, HTTPS 역방향 프록시 또는 VPN을 구성한다.
- 포트 5000을 인터넷에 직접 공개하지 않는다.
- 토양 수분 센서의 건조·습윤 원시값을 실제 배지에서 보정한다.
- Discord 이상·복구·오프라인·재시도 시나리오를 실제 네트워크에서 점검한다.
- 현재 변경 파일을 검토한 뒤 Git에 커밋하고 원격 저장소에 백업한다.

### 하드웨어 관련 남은 과제

- 정확한 lux 측정과 작물별 조도 관측을 위해 BH1750 같은 조도 센서를 도입한다.
- 급수 상태를 관측할 필요가 있다면 장치 제어 없이 유량, 수위 또는 누수 센서 데이터를 추가 수집한다.
- 센서별 편차와 설치 위치를 반영해 현장 보정 및 장기 안정성 시험을 수행한다.

### 운영 관련 남은 과제

- 별도 장비나 외부 서비스가 `/api/status`를 감시하도록 구성해 Raspberry Pi 전원·프로세스·회선 장애까지 탐지한다.
- retention 작업을 cron 또는 systemd timer로 정기 실행한다.
- `sensor_data.db`와 `smart_farm.db`를 정기 백업하고 실제 복원 테스트를 한다.
- 로그 회전, 디스크 임계치 경보, 시간 동기화 상태를 운영 절차에 포함한다.
- webhook은 현재 DB에 저장할 수 있으므로, 더 높은 보안이 필요하면 환경변수 또는 별도 비밀 저장소로 일원화한다.

### 소프트웨어 향후 개선 후보

- 현재 메모리에만 있는 ESP32 재전송 큐를 비휘발성 저장소로 확장해 보드 재부팅에도 보존한다.
- 동기식 과거 관수 분석과 유지보수 작업을 백그라운드 작업 큐로 전환한다.
- 단일 대형 대시보드 HTML/JavaScript를 화면 컴포넌트와 모듈 단위로 분리한다.
- 사용자별 계정, 역할, 감사 로그를 도입해 다중 운영자 환경을 지원한다.
- 센서 추세 예측과 이상 패턴 탐지는 충분한 현장 데이터가 쌓인 뒤 단계적으로 적용한다.

### 권장 도입 단계

```text
1단계: 센서 모니터링 안정화
  → 2단계: Discord 이상·복구·오프라인 알림 검증
  → 3단계: 실제 재배 환경에서 센서 보정 및 장기 수집
  → 4단계: 외부 상태 감시와 정기 백업 구성
  → 5단계: 장기 데이터 기반 이상 탐지 및 예측 기능
```

## 발표 마무리 문장

이번 개발을 통해 이 프로젝트는 **센서값을 안정적으로 수집하고, 임계값 이탈과 장치 장애를 Discord로 알리며, 모든 이상·복구 이력을 대시보드에 남기는 스마트팜 모니터링 시스템**으로 목적을 명확히 했다.
