#pragma once

// Compile-only placeholders. Never flash this fixture to an actual device.
const char* WIFI_SSID = "compile-test-only";
const char* WIFI_PASSWORD = "compile-test-only";
const char* SERVER_URL = "http://127.0.0.1:5000/api/sensor";
const char* THRESHOLDS_URL = "http://127.0.0.1:5000/api/thresholds";
const char* DEVICE_ID = "compile-test";
const char* API_KEY = "";
