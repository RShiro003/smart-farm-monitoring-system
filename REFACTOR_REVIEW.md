# 리팩토링 및 검증 기록

검증일: 2026-09-17. 공동 작업자의 `cd1f3c9` 변경을 병합한 로컬 작업 트리 기준.

## 범위와 원칙

- 스마트팜의 센서 수집·원격 조회·이상/복구 기록·Discord 알림이라는 목적을 유지했다.
- 새 사용자 기능, 장치 제어, API 경로, DB 스키마 변경은 추가하지 않았다.
- 중단 직전보다 앞 단계인 DB 연결 공통화와 입력 검증부터 재검토했다.
- 기존 로컬 수정(7~8월 알림 날짜 범위 조회, 페이지 이동, CSV 필터, 기간별 차트 응답 보호)과 공동 작업자의 `appendTextCell()` 반환값 수정을 보존했다. 이 기능들은 이번 리팩토링에서 새로 추가한 기능이 아니다.
- 서버 진입점, 라우트, 서비스, 대시보드, 두 ESP32 소스와 배포 설정을 검토했다. 외부 Chart.js 배포 파일, 개인 비밀 설정, DB 데이터 자체는 리팩토링하지 않았다.

## 변경 내용

### 공통 코드 정리

- `app/services/database.py`: SQLite 연결, Row 설정, WAL, 5초 busy timeout 공통화. 초기 설정 중 실패하면 연결을 닫는다. 트랜잭션의 commit/rollback과 정상 종료 책임은 기존 호출자가 유지한다.
- 센서·알림·작물·임계값·장치·생육 서비스는 각자의 `DB_FILE`을 호출 시점에 넘긴다. 테스트 및 배포의 경로 재지정 동작을 유지했다. 작업 기록과 보존 정책은 기존처럼 센서 서비스의 연결을 재사용한다.
- `app/services/validation.py`: 유한 숫자 변환과 페이지 수 정규화를 공통화했다. 기존 숫자 문자열 허용과 페이지 상한/기본값을 유지했다.
- `app/routes/query_filters.py`: 알림 표와 CSV의 상태·항목·날짜·시간 조건 파싱을 공유한다. 기존 단일 날짜와 시작/종료 날짜 필터를 모두 유지했다.
- 장치 목록에서 매 장치마다 만들던 수신 장치 집합을 한 번만 만든다. 일회성 iterator도 목록에 빠짐없이 반영한다.

### 예외 처리 보완

- 알림 설정의 NaN/무한대, 잘못된 장치 ID 자료형, 작업 종류 배열/객체를 서버 오류 대신 400으로 거부한다.
- 보존 정책 요청이 배열·문자열·숫자·불리언이면 정리 작업을 호출하지 않는다. 정상적인 빈 객체 요청은 기존 기본 정책을 계속 사용한다.
- 작물 배정의 잘못된 장치 ID와 불리언 작물 ID를 거부한다. 생육 키에 극단적으로 큰 숫자가 들어와도 검증 오류로 응답한다.
- 장치 인증에서 배열/객체 ID 조회로 발생하던 오류를 막았다. 비ASCII 키는 UTF-8 바이트로 비교하며, 잘못된 유니코드와 세션 문자열은 인증 실패로 처리한다.
- 무한대/NaN 환경변수는 알림·오프라인·보존 정책의 기본값으로 되돌린다. 매우 큰 재알림 간격의 timedelta 생성 오버플로를 피한다.

### 대시보드 분리

- `app/templates/index.html`: 화면 구조 및 모달.
- `app/templates/_dashboard_styles.html`: 기존 스타일 블록.
- `app/templates/_dashboard_script.html`: 기존 스크립트와 장치 ID의 안전한 JSON 주입.

Jinja include로 원래 위치에 삽입한다. 외부 스크립트 로딩이나 실행 시점 변경은 없다. 분리 전 내용을 복원하여 줄바꿈 정규화 및 끝 공백 제외 기준으로 문자 수(155,292)와 체크섬의 일치를 확인했다. CSS와 JavaScript 동작 코드는 이동만 했다.

## 검증 결과

- 기준 테스트: 182개 통과.
- 최종 Python 테스트: 203개 통과(기존 182개 + 회귀 테스트 21개).
- 대시보드 JavaScript 실행 테스트: 12개 통과. Node의 DOM/Chart 모형을 사용하며 실제 브라우저 시각 검증을 대신하지는 않는다.
- `app.main` 패키지 실행과 `app` 폴더의 `main` 실행 방식 모두 임시 DB로 상태 API 및 대시보드 응답 확인.
- 실제 센서 ESP32 및 더미 ESP32의 PlatformIO 컴파일 성공. 비밀 설정이 없어 `tests/fixtures/firmware/secrets.h`의 가짜 설정으로 빌드했다. 펌웨어 소스·핀 배치·측정 주기는 바꾸지 않았다.
- 충돌 표시 없음. `git diff --check` 통과. LF/CRLF 경고는 충돌이나 테스트 실패가 아니다.

| 검증 영역 | 확인한 주요 동작 | 방법 |
|---|---|---|
| 센서 수집 | 숫자/범위 검증, DHT 일부 실패, sample_id 중복 방지, 기존 요청/응답 호환 | Python 임시 DB 테스트 |
| 조도 | digital/lux 저장·추론·이전 데이터 분류, 디지털 값의 lux 판정 및 집계 제외 | Python + JS |
| 알림 | 이상·복구, 쿨타임, 연속 횟수·지속 시간, 위험 즉시 알림, 설정 상속 | Python |
| 알림 조회 | 장치·상태·항목·날짜·시간, 7~8월 범위, 정렬, 페이지 경계, API 별칭 | Python + JS |
| 오프라인 | 마지막 수신, 미수신 등록 장치, 복구, 재알림 제한, watchdog 중복 시작 방지 | Python |
| Discord | 전송 호출, 실패 outbox 보존, 장치별 웹훅, 일간 요약 중복 큐잉 방지 | 모의 전송을 사용하는 Python 테스트 |
| 임계값·작물 | 작물 범위 적용, ESP32 조회 일치, 토양 보정 유지, 잘못된 입력 거부 | Python |
| 대시보드 | 기존 패널, 차트 기간, 늦은 응답 무시, 히스토리 렌더링 반환값, 페이지 이동, 요청 취소·401 | Python 렌더링 + JS 실행 |
| 장치 관리·인증 | 등록/수정/삭제, 원본 기록 유지, API 키·Bearer·세션, 장치 키 제한, 비밀값 미노출 | Python |
| 생육·작업·관수 추정 | 기록 API, 일별 분석, 관수 추정의 스파이크/보정/시간 공백 제외, 과거 재계산 및 동시 실행 | Python |
| CSV | 헤더/BOM, 조건 일치, 스트리밍, 수식 문자열 처리 | Python + JS |
| 저장소 | 시간 집계, 미완료 구간 보호, 보존 정책, 집계 후 장기 차트/분석 조회 | 임시 DB에서 Python |
| 원격 제어 제외 | 제어 패널이 없고 `/api/actuators`가 404인 기존 상태 | Python |

관수 추정과 사용자가 수행한 급수 작업 기록은 장치를 제어하는 기능이 아니므로 그대로 유지했다.

## 재실행

프로젝트 루트에서:

```powershell
python -B -m unittest discover -s tests
node --test tests/test_dashboard_js.cjs
```

비밀 설정 없는 환경에서 컴파일만 확인하려면 별도 PowerShell에서:

```powershell
$env:PLATFORMIO_BUILD_FLAGS = '-I../tests/fixtures/firmware'
pio run -d esp32-real-sensor-node
pio run -d esp32-dummy-node
Remove-Item Env:PLATFORMIO_BUILD_FLAGS
```

테스트용 펌웨어를 실제 장치에 업로드하지 않는다. 실제 배포 빌드는 올바른 로컬 `secrets.h`를 사용해야 한다. 이 검증에서는 장치 업로드·외부 Discord 발송·Pi 배포를 하지 않았다.

## 실환경에서 남은 확인

자동화된 검증 범위에서는 실패가 없지만 모든 하드웨어·네트워크·예외 상황이 검증됐다는 뜻은 아니다.

1. Pi DB를 백업한 뒤 배포한다. 새로운 공통 Python 파일과 두 템플릿 조각을 반드시 함께 포함한다. `index.html`만 복사하면 템플릿을 찾지 못한다.
2. 실제 설정으로 서버 재시작 후 센서 수신 시각과 수치가 갱신되는지 확인한다.
3. 기존 정상/이상/복구 조건을 사용해 Discord 수신과 대시보드 기록이 함께 생성되는지 확인한다. 임계값을 시험용으로 변경했다면 기존 값을 기록하고 시험 후 복원한다.
4. 안전한 시험 장치의 Wi-Fi 연결을 잠시 끊고 오프라인/복구 알림을 확인한다.
5. 실제 DB에 존재하는 7~8월 기간을 조회하고 CSV와 대조한다. 이번 테스트는 합성 데이터이며 운영 DB에 해당 행이 존재함을 증명하지 않는다.
6. Pi 시간대·서비스 환경변수와 일간/주간 요약 설정을 확인하고 예약 시각의 실제 Discord 수신을 확인한다.
7. 실제 브라우저에서 장치 선택, 차트, 필터, 각 설정 폼, 모바일 화면을 확인한다. 이번에는 픽셀 단위 레이아웃·실브라우저 클릭 전 과정을 검증하지 않았다.

운영 규모에서의 장시간 부하, 네트워크 장애·재접속, SD 카드 오류, 실제 센서 보정 정확도도 별도 실환경 검증 대상이다.

## Git 작업 주의

- 커밋·푸시·stash 삭제는 수행하지 않았다.
- 작업 시작 전부터 스테이징된 `app/data/sensor_data.db` 삭제와 기존 추적 pycache 변경을 그대로 두었다. 이번 리팩토링에서 DB를 삭제한 것이 아니다. 커밋 전에 의도된 변경인지 별도로 확인한다.
- `.claude/` 등 기존 개인 작업 파일을 일괄 커밋하지 않는다. 비밀 설정 및 테스트용 설정을 실제 운영 설정과 혼동하지 않는다.
