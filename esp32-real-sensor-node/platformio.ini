; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32dev_real]
; 실제 센서 노드를 빌드/업로드하기 위한 PlatformIO 환경이다.
; src/main.cpp는 DHT22, 토양수분, 조도 센서를 읽고 Flask 서버로 JSON을 POST한다.
platform = espressif32
board = esp32dev
framework = arduino
; Serial Monitor 출력 속도는 코드의 Serial.begin(115200)과 맞춰야 로그가 깨지지 않는다.
monitor_speed = 115200
upload_speed = 115200

board_build.flash_mode = dio
board_build.f_flash = 40000000L

lib_deps =
    ; DHT22 온습도 센서 읽기용 라이브러리
    adafruit/DHT sensor library
    adafruit/Adafruit Unified Sensor
    ; /api/thresholds 응답 JSON을 파싱해 ESP32 LED 판단 임계값으로 사용한다.
    bblanchon/ArduinoJson
